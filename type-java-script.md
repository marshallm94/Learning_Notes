# [Java | Type]Script

## Questions

- When to use `type` vs when to use `interface`?
- `const` vs `let` vs `var`
- Why are closures (seemingly) highly used? is this a TS/JS thing, or just a general web-dev thing?
- How many different ways are there to define a function? Why are there so many different ways? Are some more common than others?
- Callbacks?
- React/web-dev:
  - What is the `useState()` function?


